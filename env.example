# MongoDB Configuration
# Default: Uses MongoDB Atlas (cloud database)
# To use local MongoDB: Change to mongodb://mongodb:27017/modbus-monitor
MONGO_URI=

# Modbus Slave Configuration
SLAVE_ADDRESS=1
